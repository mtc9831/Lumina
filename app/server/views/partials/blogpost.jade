ul.inline
    li(ng-if='posts.length == 0') There is no post.
    li(ng-if='posts.length == 1') There is {{ posts.length }} post.
    li(ng-if='posts.length > 1') There are {{ posts.length }} posts.
div(ng-repeat='post in posts')
  h3 {{post.title}}
  h5 Posted on: {{post.postTime}}
  div {{post.postText}}
  a(href='/readPost/{{post._id}}') More
  |  - 
  a(href='/editPost/{{post._id}}') Edit
  |  - 
  a(href='/deletePost/{{post._id}}') Delete